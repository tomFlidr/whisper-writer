<Window x:Class="WhisperWriter.Views.HistoryWindow"
		xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
		xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
		Title="Transcription History"
		Width="520" Height="480"
		WindowStyle="None"
		AllowsTransparency="True"
		Background="Transparent"
		WindowStartupLocation="CenterScreen"
		ResizeMode="CanResizeWithGrip">

	<Border Background="{StaticResource BgBrush}"
			CornerRadius="14"
			MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
		<Border.Effect>
			<DropShadowEffect Color="Black" BlurRadius="24" ShadowDepth="6" Opacity="0.55"/>
		</Border.Effect>

		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition Height="44"/>
				<!-- title bar -->
				<RowDefinition Height="*"/>
				<!-- list -->
			</Grid.RowDefinitions>

			<!-- Title bar -->
			<Grid Grid.Row="0" Margin="16,0">
				<TextBlock Text="History"
						   Foreground="{StaticResource TextPrimaryBrush}"
						   FontSize="14" FontWeight="SemiBold"
						   FontFamily="Segoe UI"
						   VerticalAlignment="Center"/>
				<Button Style="{StaticResource IconButton}"
						Content="✕"
						HorizontalAlignment="Right"
						VerticalAlignment="Center"
						Click="BtnClose_Click"/>
			</Grid>

			<Rectangle Grid.Row="0" VerticalAlignment="Bottom" Height="1"
					   Fill="#22FFFFFF" Margin="0"/>

			<!-- List -->
			<ListView x:Name="HistoryList"
					  Grid.Row="1"
					  Background="Transparent"
					  BorderThickness="0"
					  Foreground="{StaticResource TextPrimaryBrush}"
					  FontFamily="Segoe UI"
					  FontSize="13"
					  Margin="8,4"
					  ScrollViewer.HorizontalScrollBarVisibility="Disabled"
					  SelectionMode="Single">
				<ListView.ItemContainerStyle>
					<Style TargetType="ListViewItem">
						<Setter Property="Padding" Value="10,8"/>
						<Setter Property="Margin" Value="0,2"/>
						<Setter Property="Background" Value="Transparent"/>
						<Setter Property="BorderThickness" Value="0"/>
						<Setter Property="Template">
							<Setter.Value>
								<ControlTemplate TargetType="ListViewItem">
									<Border x:Name="Bd"
											Background="{TemplateBinding Background}"
											CornerRadius="8"
											Padding="{TemplateBinding Padding}">
										<ContentPresenter/>
									</Border>
									<ControlTemplate.Triggers>
										<Trigger Property="IsMouseOver" Value="True">
											<Setter TargetName="Bd" Property="Background" Value="#1AFFFFFF"/>
										</Trigger>
										<Trigger Property="IsSelected" Value="True">
											<Setter TargetName="Bd" Property="Background" Value="#2A8B8BFF"/>
										</Trigger>
									</ControlTemplate.Triggers>
								</ControlTemplate>
							</Setter.Value>
						</Setter>
					</Style>
				</ListView.ItemContainerStyle>
				<ListView.ItemTemplate>
					<DataTemplate>
						<Grid>
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="*"/>
								<ColumnDefinition Width="Auto"/>
							</Grid.ColumnDefinitions>
							<Grid Grid.Column="0" Margin="0,0,8,0">
								<Grid.RowDefinitions>
									<RowDefinition/>
									<RowDefinition/>
								</Grid.RowDefinitions>
								<TextBox Grid.Row="0"
										 Text="{Binding Text, Mode=OneWay}"
										 Foreground="{StaticResource TextPrimaryBrush}"
										 Background="Transparent"
										 BorderThickness="0"
										 IsReadOnly="True"
										 TextWrapping="Wrap"
										 MaxHeight="60"
										 IsTabStop="False"
										 FocusVisualStyle="{x:Null}"
										 Cursor="IBeam"
										 FontFamily="Segoe UI"
										 FontSize="13"/>
								<TextBlock Grid.Row="1"
										   Foreground="{StaticResource TextSecondaryBrush}"
										   FontSize="11"
										   Margin="0,3,0,0">
									<TextBlock.Text>
										<MultiBinding StringFormat="{}{0:HH:mm:ss}  &#x2022;  {1:0.0}s">
											<Binding Path="Timestamp"/>
											<Binding Path="Duration.TotalSeconds"/>
										</MultiBinding>
									</TextBlock.Text>
								</TextBlock>
							</Grid>
							<Button Grid.Column="1"
									Style="{StaticResource IconButton}"
									Content="Copy"
									VerticalAlignment="Center"
									Click="BtnCopyEntry_Click"/>
						</Grid>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</Grid>
	</Border>
</Window>